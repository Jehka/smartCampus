package campus.users;

/**
 * Part 2: Marker interface — classes implementing this get "special privileges".
 * No methods; presence alone signals secure access.
 */
public interface SecureAccess {
    // marker — intentionally empty
}
